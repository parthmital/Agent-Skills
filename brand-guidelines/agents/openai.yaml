interface:
  display_name: "Brand Guidelines"
  short_description: "Brand voice, identity, and consistency"
  default_prompt: "Use $brand-guidelines to define, review, or apply this brand's voice, visual identity, and guidelines."
policy:
  allow_implicit_invocation: true
