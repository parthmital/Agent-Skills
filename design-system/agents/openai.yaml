interface:
  display_name: "Design System"
  short_description: "Design tokens and component specs"
  default_prompt: "Use $design-system to build design tokens, component specifications, and token validation for this project."
policy:
  allow_implicit_invocation: true
